using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ib_actLives2 : MonoBehaviour
{
    ib_LevelUI UI;
    // private bool startGame;
    // private int actLives;
    public Text actLivesText;




    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {


        //actLivesText.text = "" + (ib_Datenspeicher.lives_ - 1 + ib_StaticVar._lives); // gespeicherte Werte (Minus standardisiert gesetzte Werte) plus aktuelle Werte
    }
}

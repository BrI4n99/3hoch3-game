using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class kjg_kuhController : MonoBehaviour
{
    public GameObject kuhZiel;
    Transform target;

    // Start is called before the first frame update
    void Start()
    {
        target = kuhZiel.transform;
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
